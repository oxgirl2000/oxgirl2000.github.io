{
  "name": "oxgr.github.io",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "main": "build.js",
  "scripts": {
    "build": "node build.js",
    "watch": "nodemon --watch src -e md,html --exec \"npm run build\"",
    "serve": "browser-sync start --server --files \"**/*\"",
    "copy": "rm -rf docs && cp -r src docs && mv docs/html/* docs && rm -rf docs/html",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/oxgr/oxgr.github.io.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/oxgr/oxgr.github.io/issues"
  },
  "homepage": "https://github.com/oxgr/oxgr.github.io#readme",
  "devDependencies": {
    "@11ty/eleventy": "^1.0.2"
  },
  "dependencies": {
    "fs-extra": "^10.1.0",
    "nodemon": "^2.0.20",
    "showdown": "^2.1.0"
  }
}
