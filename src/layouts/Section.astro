---
interface Props {
  title: string,
}

const { title } = Astro.props;
---

<section>
  <h2 class="no-box">{title}</h2>
  <slot/>
</section>

<style>
  section {
    margin-bottom: 4rem;
    /* border: solid 2px red; */
  }
  h2 {
    color: var(--ox-orange);
  }
</style>
